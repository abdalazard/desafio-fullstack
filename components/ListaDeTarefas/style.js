import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  viewContainer: {
    width: '100%',
    alignItems: 'center',
    marginBottom: 20
  },
});

export default styles;
